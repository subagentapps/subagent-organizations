node_modules/
dist/
.DS_Store
*.log
.env
.env.*
!.env.example
coverage/
.turbo/
# vendor/ — gitignored per CLAUDE.md #5 ("Don't add submodules"). Used by
# src/manifest/sync.ts as a clone-on-demand cache. Anything written here is
# transient and should not be tracked.
vendor/
