node_modules/
dist/
.DS_Store
*.log
.env
.env.*
!.env.example
coverage/
.turbo/
# vendor/ — REMOVED. The vendor/anthropic/ tree is tracked as git submodules.
# vendor/lsp/ holds vendored static spec snapshots; tracked.
# Add per-path entries here if you create transient vendor dirs that shouldn't be tracked.
