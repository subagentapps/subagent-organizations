node_modules/
dist/
.DS_Store
*.log
.env
.env.*
!.env.example
coverage/
.turbo/
# vendor/ — REMOVED. The vendor/anthropic/ tree is now tracked as git submodules.
# Add per-path entries here if you create transient vendor dirs that shouldn't be tracked.
